using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ch2_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int hp = 2;
        int _hp = 2;
        int HP = 2;

        float speed = 2.1f;

        string msg = "我是字串";

        bool isOpen = false;

        Debug.Log("測試 _hp：" + _hp);
        Debug.Log("測試 speed：" + speed);
        Debug.Log("測試 isOpen：" + isOpen);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
